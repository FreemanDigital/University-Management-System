<%= form_with(model: office) do |form| %>
  <% if office.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(office.errors.count, "error") %> prohibited this office from being saved:</h2>

      <ul>
        <% office.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :building_name %>
    <%= form.text_field :building_name, class: "form-control w-50" %>
  </div>

  <div class="form-group">
    <%= form.label :floor_number %>
    <%= form.number_field :floor_number, class: "form-control w-25"  %>
  </div>

  <div class="form-group">
    <%= form.label :room_number %>
    <%= form.number_field :room_number, class: "form-control w-25"  %>
  </div>

  <div class="actions mt-3 mb-1">
    <%= form.submit class: "btn btn-secondary" %>
  </div>
<% end %>
