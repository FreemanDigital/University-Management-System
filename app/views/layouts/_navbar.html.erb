<script>
  $(document).ready(function() {
    $(".dropdown-toggle").show();
  })
</script>
<div class="bg-dark">
  <nav class="navbar navbar-expand-lg navbar-dark mx-auto" style="max-width: 1300px;">
    <div class="container-fluid">
      <a class="navbar-brand" href="/"><%= image_tag "logo.png", :size => "100x100" %></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarColor01">
        <% if user_signed_in? %>
          <div class="dropdown me-2">
            <a class="btn btn-secondary dropdown-toggle" type="button" id="resourceDrop" data-bs-toggle="dropdown" aria-expanded="false">
              Resources
            </a>
            <ul class="dropdown-menu" aria-labelledby="resourceDrop">
              <li class="nav-item">
                <%= link_to "Teachers", :teachers, {:class => "nav-link"} %>
              </li>
              <li class="nav-item">
                <%= link_to "Offices", :offices, {:class => "nav-link"} %>
              </li>
              <li class="nav-item">
                <%= link_to "Courses", :courses, {:class => "nav-link"} %>
              </li>
              <li class="nav-item">
                <%= link_to "Sections", :sections, {:class => "nav-link"} %>
              </li>
            </ul>
          </div>
        <% end %>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <% if user_signed_in? %>
            <li class="nav-item">
              <%= link_to "Edit Profile", edit_user_registration_path, {:class => "btn btn-warning me-2"} %>
            </li>
            <li class="nav-item">
              <%= link_to "Log Out", destroy_user_session_path, {method: :delete, :class => "btn btn-warning me-2"} %>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to "Sign Up", new_user_registration_path, {:class => "btn btn-warning me-2"} %>
            </li>
            <li class="nav-item">
              <%= link_to "Sign In", new_user_session_path, {:class => "btn btn-warning me-2"} %>
            </li>
          <% end %>
        </ul>
      </div>
      <%
=begin%>
 <div class="d-flex justify-content-between">
        <div class="mt-3">
          <%= form_tag(search_url, method: "get") do %>
            <%= label_tag(:query, "Search for:") %>
            <%= text_field_tag(:query) %>
            <%= submit_tag("Search") %>
          <% end %>
        </div>
      </div> 
<%
=end%>
    </div>
  </nav>
</div>